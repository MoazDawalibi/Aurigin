# Leave empty in local Vite dev to use /api with the proxy in vite.config.ts.
# For production, set this to the deployed Laravel API base URL, for example:
# VITE_API_BASE_URL=https://api.aurigin.example/api
VITE_API_BASE_URL=
